app:
  image:
    repository: pipeline-demo-app
    tag: "v1.0.0"
  replicas: 3
  env: production
  resources:
    requests:
      cpu: "200m"
      memory: "256Mi"
    limits:
      cpu: "1000m"
      memory: "1Gi"

postgres:
  password: "REPLACE_IN_CI"
  databaseUrl: "REPLACE_IN_CI"

ingress:
  host: app.prod.example.com
